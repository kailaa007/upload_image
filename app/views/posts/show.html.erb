<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>
  <%= image_tag(@post.title, width: 200) if @post.title.present? %>

<p>
  <strong>Detail:</strong>
  <%= @post.detail %>
</p>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
